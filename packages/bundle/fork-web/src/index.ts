/**
 * @knyazevai/dsh-fork-web — the opt-in Web composition layer. Its patch
 * mounts the fork Workspace UI browser plugin; upstream Web and fork Host
 * rows remain owned by their own bundle packages.
 *
 * @module @knyazevai/dsh-fork-web
 */

export {}
