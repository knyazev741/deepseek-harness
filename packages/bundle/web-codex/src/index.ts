/**
 * @deepseek-ai/dsh-web-codex — an opt-in profile bundle whose behavior lives
 * in `cordis.patch.yml`; the patch mounts the external-session family once
 * over the Web application rows.
 * @module @deepseek-ai/dsh-web-codex
 */

export {}
