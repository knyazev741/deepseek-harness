/**
 * Web new-session mode picker: the "模式" seat choosing between the native DSH
 * agent loop and registered external console agents (Codex, …), plus the
 * per-mode model seat. Scaffolding commit: the mode picker component and its
 * slot registration land in the following increment; this apply registers the
 * client bundle so the package is present and buildable.
 */
import type { Context } from '@deepseek-ai/cordis'

/** Client plugin body — mode picker component and slot registration follow. */
export function apply(_ctx: Context): void {}
